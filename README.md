# Git_course
for enhancing some skills

## Project notes:

git clone <http url>
mkdir
cd
git status


---
git add <file name >
git add *
---

git reset head <file name>


git commit -m "<the message>"
git branch
git remote -v
git push <Remote repo name> <Branch name>
